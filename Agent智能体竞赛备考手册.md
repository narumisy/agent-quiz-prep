# Agent 智能体竞赛备考手册

> 考试时间：8月21日 ｜ 考试时长：90 分钟
> 初赛城市：柳州、青岛、重庆、上海（集中笔试）

---

## 考试概况与分值分布

| 题型 | 题量 | 每题分值 | 小计 |
|------|------|----------|------|
| 单选题 | 70 题 | 1 分 | 70 分 |
| 多选题 | 10 题 | 2 分 | 20 分 |
| 判断题 | 20 题 | 0.5 分 | 10 分 |
| **合计** | 100 题 | — | **100 分** |

- 笔试成绩前 **30 名**晋级决赛。
- 无倒扣分说明，答题时应尽量作答；多选题需注意"选全"与"选对"的权衡（若题目注明"多选、少选、错选均不得分"，则宁少勿错；若注明"少选得分"，则选把握最大的选项）。

## 答题时间分配建议（90 分钟）

| 题型 | 建议用时 | 说明 |
|------|----------|------|
| 单选题 70 题 | 约 40 分钟 | 平均 35 秒/题，不会的先标记跳过 |
| 判断题 20 题 | 约 10 分钟 | 注意绝对化措辞（"一定""必须""所有"多为错） |
| 多选题 10 题 | 约 20 分钟 | 平均 2 分钟/题，最易失分，认真辨析 |
| 检查/涂卡 | 剩余 15-20 分钟 | 重点复核多选与标记的题目 |

---

# 第一部分 基础知识（约 30%）

## 1.1 计算机操作系统

### 概念与功能
- **操作系统（OS）**：管理计算机硬件与软件资源的系统软件，是用户与硬件之间的接口。
- 核心功能：**进程管理、内存管理、文件系统管理、设备管理、用户接口（CLI/GUI）**。
- 常见 OS：Windows、Linux、macOS、Android、iOS、Unix。
- 分时系统让多个用户共享计算机资源；实时系统强调响应时限。

### 进程与线程
- **进程**：程序的一次执行过程，是**资源分配**的基本单位，有独立地址空间。
- **线程**：进程内的执行流，是 **CPU 调度**的基本单位，共享所属进程的资源。
- 进程三态：**就绪、运行、阻塞**（等待 I/O 或事件）。
- 常见概念：并发（宏观并行、微观交替）、并行（真正同时执行）、死锁、临界区、信号量、同步与互斥。

### 内存管理
- 虚拟内存、分页/分段、页面置换算法（LRU 等）。
- 缓存（Cache）介于 CPU 与内存之间，速度高于内存。

### 考点提示
- 判断题易考："进程是 CPU 调度基本单位"（**错**，是线程）；"操作系统属于应用软件"（**错**，是系统软件）。

## 1.2 计算机软件

- 软件 = **程序 + 文档 + 数据**。
- 分类：
  - **系统软件**：操作系统、编译程序、数据库管理系统、驱动程序、汇编程序。
  - **应用软件**：办公软件、浏览器、图像处理工具、行业软件。
- **编译**：一次性把高级语言整个翻译为机器码（如 C/C++）；**解释**：逐行翻译执行（如 Python、旧版 BASIC）。
- 软件生命周期：需求分析 → 设计 → 编码 → 测试 → 运行维护。
- 开源软件：源代码公开，受开源许可证约束。

## 1.3 计算机硬件

- **冯·诺依曼体系**：存储程序原理，五大部件 = **运算器、控制器、存储器、输入设备、输出设备**。
- **CPU** = 运算器 + 控制器 + 寄存器（含 Cache），性能指标：主频（Hz）、核心数、字长。
- 存储器层次：寄存器 → Cache → 内存(RAM) → 外存(硬盘/SSD)，速度递减、容量递增、价格递减。
- **RAM**（随机存取存储器，断电丢失）与 **ROM**（只读，断电不丢失）的区别是高频考点。
- 单位换算：1 Byte = 8 bit；1 KB = 1024 B；1 MB = 1024 KB；1 GB = 1024 MB；1 TB = 1024 GB。
- 输入设备：键盘、鼠标、扫描仪；输出设备：显示器、打印机、音箱。

## 1.4 计算机安全

### 常见威胁
- 病毒、木马（Trojan）、蠕虫（Worm）、勒索软件（Ransomware）、钓鱼（Phishing）、SQL 注入、XSS、DDoS 攻击。
- **木马**不自我复制，伪装成正常程序窃取数据；**蠕虫**通过网络自我复制传播，不依附宿主文件。

### 安全技术与机制
- 防火墙（网络边界防护）、入侵检测（IDS）/入侵防御（IPS）、杀毒软件、数据加密、访问控制。
- **认证三要素**：口令、令牌/智能卡、生物特征。
- 加密算法：
  - 对称加密（加解密用同一密钥）：AES、DES、3DES。
  - 非对称加密（公钥/私钥）：RSA、ECC；公钥加密、私钥解密，私钥签名、公钥验签。
  - 哈希算法（不可逆、防篡改）：MD5、SHA 系列；用于完整性校验与密码存储。
- HTTPS = HTTP + **SSL/TLS**，通过证书验证身份并加密传输。

### 网络安全等级
- 等保（信息安全等级保护）是合规要求；最小权限原则、纵深防御。

## 1.5 计算机网络

### OSI 七层模型（记忆：应物表会传网数物 → 自下而上）
| 层次 | 作用 | 典型协议/设备 |
|------|------|--------------|
| 应用层 | 为用户提供服务 | HTTP、HTTPS、FTP、SMTP、DNS、Telnet |
| 表示层 | 数据格式与加密 | JPEG、ASCII、SSL（部分教材归此） |
| 会话层 | 建立/管理会话 | NetBIOS |
| 传输层 | 端到端可靠传输 | **TCP / UDP**、端口 |
| 网络层 | 路由寻址 | **IP**、ICMP、路由器 |
| 数据链路层 | 相邻节点帧传输 | 以太网、MAC 地址、交换机 |
| 物理层 | 比特流传输 | 网线、集线器 |

- **TCP**：面向连接、可靠（三次握手建立、四次挥手断开）、有流量控制和拥塞控制；适用于网页、文件传输。
- **UDP**：无连接、不可靠、开销小、实时性好；适用于视频直播、语音、DNS。
- 常用端口：HTTP=80、HTTPS=443、FTP=21、SSH=22、DNS=53、SMTP=25、MySQL=3306。
- **IP 地址**：IPv4 32 位（点分十进制），IPv6 128 位。私有地址段：10.x、172.16-31.x、192.168.x。
- **DNS**：将域名解析为 IP 地址。
- 局域网（LAN）/城域网（MAN）/广域网（WAN）。

### 移动互联网（并入本节）
- 移动通信代际：4G（LTE）→ 5G（高带宽、低时延、海量连接）。
- 5G 三大应用场景：增强移动宽带（eMBB）、超可靠低时延（uRLLC）、海量机器类通信（mMTC）。
- 移动互联网特点：便携性、即时性、位置感知、个性定制。

## 1.6 云计算与大数据

### 云计算
- 定义：通过网络按需提供可配置计算资源的模式。
- **三种服务模式**（高频考点）：
  - **IaaS**（基础设施即服务）：提供虚拟机、存储、网络——用户管 OS 及以上，如 AWS EC2、阿里云 ECS。
  - **PaaS**（平台即服务）：提供开发运行平台——用户只管应用，如云数据库、Docker 平台。
  - **SaaS**（软件即服务）：直接提供软件应用，如 Office 365、钉钉、在线文档。
- 四种部署模型：公有云、私有云、混合云、社区云。
- 关键技术：虚拟化、分布式计算、容器化（Docker/K8s）、Serverless。

### 大数据
- **4V 特征**：**Volume（海量）、Velocity（高速）、Variety（多样）、Value（价值密度低）**（部分教材加 Veracity 真实性）。
- 处理流程：采集 → 清洗 → 存储 → 分析 → 可视化。
- 典型技术：Hadoop（HDFS 分布式存储 + MapReduce 计算）、Spark（内存计算）、Flink（流计算）、HBase、数据仓库。
- **结构化数据**（表格）、**半结构化**（JSON/XML）、**非结构化**（文本/图片/视频）。

## 1.7 人工智能基础

- **AI（人工智能）> 机器学习（ML）> 深度学习（DL）**：包含关系。
- 机器学习三类型：
  - **监督学习**：有标注数据，如分类、回归（垃圾邮件分类、房价预测）。
  - **无监督学习**：无标注，如聚类、降维（用户分群）。
  - **强化学习**：通过奖励/惩罚与环境交互学习策略（AlphaGo、游戏 AI）。
- 神经网络基础：输入层、隐藏层、输出层；**激活函数**（ReLU、Sigmoid）；损失函数；梯度下降与反向传播。
- 深度学习框架：TensorFlow、PyTorch、PaddlePaddle。
- 自然语言处理（NLP）：分词、词向量、注意力机制（Attention）。
- **大语言模型（LLM）**：基于 Transformer 架构（自注意力机制）的生成式模型，通过海量文本预训练，具备上下文理解与生成能力。代表：GPT、DeepSeek、Claude、Qwen。
- 生成式 AI：能生成文本、图像、代码等新内容。
- 相关概念：Token、上下文窗口、微调（Fine-tuning）、Embedding（嵌入向量）、幻觉（Hallucination）。

## 1.8 图像处理基础

- 图像由**像素**组成，分辨率 = 像素总数（如 1920×1080）。
- **颜色模型**：RGB（加色，显示器）、CMYK（减色，印刷）、HSV/HSL、灰度（Gray）。
- 常见格式：BMP（无损未压缩）、JPEG（有损压缩，适合照片）、PNG（无损，支持透明）、GIF（支持动画，最多 256 色）、TIFF、WebP。
- 基本操作：缩放、旋转、裁剪、滤波（模糊/锐化）、直方图均衡化、边缘检测。
- 深度学习图像任务：图像分类、目标检测（YOLO）、图像分割、图像生成（GAN、扩散模型）。
- 常用库：OpenCV、PIL/Pillow、scikit-image、Matplotlib（可视化）。

## 1.9 Python 基础

### 语法与特性
- 解释型、动态类型、缩进表示代码块；支持面向对象、函数式。
- 内置数据结构：
  - **list**（列表）：有序、可变，`[]`。
  - **tuple**（元组）：有序、**不可变**，`()`。
  - **dict**（字典）：键值对、可变、键唯一，`{}`。
  - **set**（集合）：无序、**元素唯一**，用于去重与集合运算。
- 流程控制：`if/elif/else`、`for`、`while`；函数用 `def`；类用 `class`。
- 常见内置函数：`len()`、`range()`、`type()`、`sorted()`、`enumerate()`、`zip()`、`map()`、`filter()`。
- 常用标准库/第三方库：`os`、`sys`、`json`、`re`（正则）、`datetime`、`requests`（HTTP）、`numpy`、`pandas`（数据分析）、`matplotlib`（绘图）、`flask`（Web）。
- 文件操作：`open(path, 'r'/'w'/'a', encoding='utf-8')`，用 `with` 语句自动关闭。
- 切片 `list[1:3]` 左闭右开；字符串不可变；`is` 比较对象身份、`==` 比较值。

### 高频易错
- 判断题：`Python 是编译型语言`（**错**，解释型）；`列表可以修改，元组不能修改`（**对**）；`字典的键必须是不可变类型`（**对**，如字符串、数字、元组）。

## 1.10 相关法律法规

### 三大法律（高频）
- **《网络安全法》**（2017）：网络安全保护、关键信息基础设施、实名制。
- **《数据安全法》**（2021）：数据分类分级、重要数据保护、数据出境安全评估。
- **《个人信息保护法》（PIPL，2021）**：个人信息处理规则、**"告知-同意"**原则、敏感个人信息保护、个人信息跨境提供。

### 其他相关法规
- **《著作权法》**：保护软件著作权；未经授权复制、传播软件构成侵权。
- **《计算机软件保护条例》**：软件受著作权保护，源代码与目标代码均受保护。
- **开源许可证**：GPL（copyleft，衍生作品须开源）、Apache、MIT（宽松许可）、BSD、LGPL。
- **《电子商务法》《反不正当竞争法》**：规范网络经营活动。
- AI 治理原则：**合法合规、公平公正、透明可解释、责任可追溯、保护隐私**。
- 数据爬取须遵守 robots 协议与平台规则，不得非法获取他人数据。

---

# 第二部分 系统设计（Agent 方向，约 35%）

> 这是初赛**最核心**的板块，分值占比最高，务必重点掌握。

## 2.1 Agent 概念

### 什么是 Agent
- **Agent（智能体）**：能够**感知（Perceive）环境、做出决策（Decide）、采取行动（Act）**，并具备**自主性、目标导向、与环境交互能力**的智能实体。
- 经典定义强调四个特征：
  - **自主性（Autonomy）**：独立决策，无需人工干预。
  - **反应性（Reactivity）**：能对环境的实时变化做出响应。
  - **主动性（Proactiveness）**：主动采取行动追求目标，而不是被动等待。
  - **社交性（Social Ability）**：能与其他 Agent 或人类交互协作。

### 基于 LLM 的 Agent 与传统 Agent 的区别
- 传统 Agent：规则驱动 / 符号推理 / 强化学习训练策略。
- **LLM-based Agent（大模型智能体）**：以 LLM 作为"大脑"（推理与决策核心），结合规划、记忆、工具使用完成复杂任务。
- **LLM 与 Agent 的关系**：LLM 是 Agent 的"引擎/推理内核"；Agent = LLM + 规划 + 记忆 + 工具 + 执行。LLM 本身不主动行动，Agent 赋予其自主执行能力。

### Agent vs LLM（高频判断）
- "LLM 本身就是一个 Agent"（**错**——LLM 只做生成/推理，缺少感知环境、调用工具、自主行动的闭环）。
- "Agent 能自主调用工具并执行多步骤任务"（**对**）。

## 2.2 Agent 架构模型

### 核心认知架构（Agent 的"感知-规划-记忆-行动"循环）
```
环境 → 感知/观察(Observation) → 规划(Planning) → 决策(Decision)
     → 行动/调用工具(Action) → 结果反馈 → 更新记忆 → 下一轮循环
```

### 主流架构/模式（高频考点）
1. **ReAct 模式**（Reasoning + Acting）
   - 交替进行"**推理（Thought）→ 行动（Action）→ 观察（Observation）**"，让模型边思考边行动，用外部信息校验推理。
   - 代表论文：ReAct: Synergizing Reasoning and Acting in Language Models。
   - 优点：可解释、可纠错；是当前多数 Agent 框架的基础。

2. **Plan-and-Execute（计划-执行）**
   - 先制定整体计划（Plan），再逐步执行（Execute），每步验证，必要时重新规划。
   - 适合复杂、长流程任务；与 ReAct 相比减少重复推理，效率更高。

3. **Reflexion（反思）**
   - 在任务失败后生成**反思（reflection）**存入记忆，下次尝试时利用经验改进，形成自我改进循环。

4. **多轮循环 / 人机协同（Human-in-the-loop）**
   - 关键节点请求人工确认或审批，保证安全与正确。

5. **分层/角色化架构**：主 Agent（Orchestrator）调度多个子 Agent（Worker/Specialist）。

### 单 Agent vs 多 Agent
- 单 Agent：一个推理循环完成所有工作，简单可控，适合单一任务。
- 多 Agent：多个 Agent 分工协作，适合复杂任务，但通信开销与协调难度更大。

## 2.3 多 Agent 协作模型（重点）

### 常用协作模式
1. **顺序流水线（Pipeline）**：Agent A 输出 → Agent B 输入 → … 线性流转。
2. **分层 / 主从（Hierarchical / Orchestrator-Worker）**：主 Agent 拆分任务、分派给子 Agent、汇总结果。
3. **群聊 / 会议（Group Chat）**：多个 Agent 在共享会话中自由发言讨论（如 AutoGen 的 GroupChat）。
4. **辩论 / 对抗（Debate / Adversarial）**：多个 Agent 持不同立场互相质询，提升答案质量（如多 Agent 辩论生成）。
5. **委派（Delegation）**：Agent 间互相委派子任务（如 LangGraph 的 Send API）。
6. **协作 / 分工（Collaboration / Role-play）**：按角色分工协作（如 MetaGPT 中的产品经理、架构师、工程师、QA 角色扮演）。

### 多 Agent 协作的关键问题
- **通信协议与消息格式**：共享的消息传递机制、结果如何汇总。
- **角色与分工设计**：职责边界清晰，避免重复劳动。
- **协调与冲突解决**：谁来决策、意见冲突时如何收敛。
- **状态管理**：全局状态与共享记忆如何维护。
- **一致性与上下文**：防止信息在传递中丢失或失真。

### 常见框架
- **AutoGen**（微软）：对话式多 Agent，GroupChat 支持群聊、辩论、分层。
- **CrewAI**：基于"角色-任务-工具"的团队协作。
- **MetaGPT**：软件公司式角色分工，多 Agent 协作生成代码。
- **LangGraph**：基于图的低层编排，可精细控制多 Agent 流程与状态。
- **AgentScope**（阿里）：分布式多 Agent 平台。

## 2.4 系统组件与关键技术

一个完整的 LLM Agent 系统通常由以下组件组成：

| 组件 | 作用 | 关键技术 |
|------|------|----------|
| **LLM 核心** | 推理、规划、生成 | Transformer、上下文管理、提示词工程 |
| **规划模块** | 任务拆解、制定计划 | ReAct、CoT、任务分解、计划生成器 |
| **记忆模块** | 保存历史与知识 | 短期记忆（上下文）、长期记忆（向量库/数据库） |
| **工具模块** | 调用外部能力 | Function Calling、API 集成、代码解释器、搜索、数据库 |
| **感知模块** | 获取外部信息 | 检索（RAG）、多模态输入（图像/语音）、文件解析 |
| **执行/行动模块** | 与环境交互 | 执行代码、操作网页、写入数据、调用业务系统 |
| **反馈/评估模块** | 校验与改进 | 观察结果、反思、人工反馈、评估指标 |

### 关键技术点
- **工具调用（Tool Use / Function Calling）**：LLM 生成结构化调用参数，系统执行外部函数并返回结果。
- **记忆（Memory）**：区分短期（对话窗口）与长期（向量检索 / 数据库），详见 4.7。
- **检索增强生成（RAG）**：检索外部知识库辅助生成，减少幻觉，详见 4.5。
- **多模态**：支持文本、图像、语音、代码等输入输出。
- **上下文管理**：上下文窗口有限，需截断、摘要、滑动窗口。

## 2.5 工作流编排（Workflow Orchestration）

### 概念
- **工作流编排**：将 Agent 任务的多个步骤（节点）组织成有向图，定义执行顺序、条件分支、并行、循环与数据流转。

### 常用编排方式
- **有向无环图（DAG）**：节点按依赖关系执行，支持并行（如 LangGraph）。
- **状态机（State Machine）**：基于状态转移控制流程。
- **顺序 / 条件分支 / 并行（fan-out/fan-in）**：按条件跳转、多路并行后汇总。
- **循环与重试**：失败重试、反思循环、人工审批节点（Human-in-the-loop）。
- **事件驱动**：由外部事件/Webhook 触发。

### Orchestration vs Choreography（易混淆）
- **Orchestration（编排）**：有一个**中心协调者**统一调度各组件（如主 Agent 分发任务）。
- **Choreography（编排/协作）**：无中心控制，各组件通过事件互相响应、自行协作（类似舞蹈各自配合）。

### 常见编排工具/平台
- LangGraph（图状态流）、Dify 工作流（可视化拖拽）、n8n、Coze/扣子、Temporal、Prefect、Airflow。
- 企业级 AI Agent 平台：Dify、Coze、FastGPT、百度千帆、阿里百炼。

### 评估一个工作流设计的关注点
- 可扩展性、可维护性、失败恢复、超时与重试、可观测性（日志与追踪）、人工介入点。

## 2.6 系统评估维度（重点）

对 Agent 系统的评估常见维度：

| 维度 | 内容 |
|------|------|
| **准确性（Accuracy）** | 任务完成正确率，输出质量 |
| **任务完成率 / 成功率** | 端到端任务是否达成目标 |
| **效率** | 完成时间、推理步数、资源消耗 |
| **成本** | Token 消耗、API 调用费用、算力开销 |
| **鲁棒性（Robustness）** | 面对输入扰动、异常、噪声时的稳定性 |
| **可靠性** | 是否稳定输出、不易崩溃或死循环 |
| **安全性** | 防止提示注入、有害输出、越权工具调用 |
| **可解释性** | 决策过程是否可追溯、可理解 |
| **可控性** | 是否易被约束、符合用户意图 |
| **用户体验** | 响应速度、交互友好度 |

### 评估方法
- **基准测试（Benchmark）**：如 HumanEval（代码）、MMLU（通用知识）、AgentBench、GAIA、SWE-bench（真实软件工程任务）。
- 自动评估（规则/LLM-as-judge）与人工评估（Human Evaluation）结合。
- 逐步评估（process）与最终结果评估（outcome）并重。
- 消融实验（Ablation）：移除某个组件看效果变化。

## 2.7 工程化与治理知识

### 工程化（Production Engineering）
- **可观测性（Observability）**：日志（Logging）、链路追踪（Tracing）、指标监控（Metrics）——记录每次 LLM 调用、工具调用、Token 消耗。
- **版本管理**：Prompt 版本、Agent 配置版本、模型版本管理；灰度发布。
- **测试**：单元测试、回归测试、评估集（Eval Set）、对抗测试。
- **持续集成/持续部署（CI/CD）**：自动化测试与发布流程。
- **缓存与性能优化**：结果缓存、并发控制、流式输出。
- **成本控制**：Token 压缩、模型分级（简单任务用小模型）、缓存。

### 治理（Governance）
- **安全治理**：权限隔离、敏感信息脱敏、工具调用白名单、防止提示注入。
- **合规治理**：数据隐私（个人信息保护法）、数据不出境、内容合规、审计留痕。
- **责任归属**：AI 决策的可追溯与人工兜底。
- **模型治理**：模型选择、更新、回滚策略。
- **数据治理**：知识库数据质量、数据血缘、定期更新。

---

# 第三部分 提示词工程（约 20%）

## 3.1 基本概念

- **提示词（Prompt）**：输入给大模型的指令/文本，用于引导模型产生期望输出。
- **提示词工程（Prompt Engineering）**：设计、优化提示词的方法论，以提升模型输出的质量、准确性与稳定性。
- 相关术语：
  - **Token**：模型处理文本的最小单位，一个中文字约 1~2 个 token，token 数影响成本与上下文容量。
  - **上下文窗口（Context Window）**：模型一次能处理的输入+输出上限。
  - **温度（Temperature）**：控制随机性，低温度输出更确定、高温度更多样。
  - **Top-p**：核采样，也控制多样性。
  - **In-context Learning（上下文学习）**：通过提示词中的示例（few-shot）让模型学会任务，无需更新参数。

## 3.2 系统提示词与用户提示词（高频考点）

### 消息结构（多轮对话）
- **System（系统提示词）**：设定模型的**角色、身份、行为准则、回答风格、边界与约束**，优先级最高，通常由开发者编写，贯穿整个会话。
- **User（用户提示词）**：用户当前提出的具体问题/任务。
- **Assistant**：模型的历史回复。

### 区别与要点
| 维度 | 系统提示词 | 用户提示词 |
|------|-----------|-----------|
| 编写者 | 开发者/系统 | 用户 |
| 作用 | 全局行为约束、角色设定 | 具体任务指令 |
| 时效 | 整个会话持续生效 | 当次请求 |
| 典型内容 | "你是一个……助手""回答要简洁""不要编造信息" | "帮我写一段周报" |

- 判断题：`系统提示词与用户提示词的优先级相同`（**错**，系统提示词优先级更高，更不易被覆盖）。
- 角色设定（Role Prompting）："你是一位资深的网络安全专家"能显著提升对应领域回答质量。
- **提示词注入（Prompt Injection）**：攻击者通过用户输入内容尝试覆盖或劫持系统提示词，是重要的安全隐患（见 3.6）。

## 3.3 思维链（Chain-of-Thought, CoT）

### 核心概念
- **思维链（CoT）**：让模型在给出最终答案前，**逐步展示中间推理过程**（"第一步……第二步……"），显著提升复杂推理任务的准确率（数学、逻辑、多步问题）。
- 本质：把隐式推理显式化，引导模型"想清楚再回答"。

### 变体（高频考点）
1. **Zero-shot CoT（零样本思维链）**：不加示例，仅加提示词"**Let's think step by step**（让我们一步步思考）"即可激发推理。
2. **Few-shot CoT（少样本思维链）**：在提示词中给出带推理过程的示例，让模型模仿。
3. **Self-Consistency（自洽性）**：多次采样思维链，对多个结果**投票取多数**，提升答案可靠性。
4. **ToT（Tree of Thoughts，思维树）**：将推理过程组织为树状分支，探索多条路径并评估，优于单链。
5. **CoT-SC / 反思**等进阶变体。

### 易错判断
- `思维链只在需要推理时才使用，普通问答用不到`（**错**——对大多数复杂任务都有效，但简单任务用 CoT 可能增加冗余）。
- `Few-shot 就是思维链`（**错**——Few-shot 是给示例，CoT 是让模型展示推理过程，二者可结合）。

## 3.4 提示词核心要素

一份高质量的提示词通常包含以下要素：

| 要素 | 说明 | 示例 |
|------|------|------|
| **角色（Role）** | 设定身份 | "你是资深的数据分析师" |
| **任务（Task）** | 明确要做什么 | "分析以下销售数据并给出结论" |
| **上下文/背景（Context）** | 提供必要信息 | 数据、约束、目标用户 |
| **示例（Examples）** | 少样本示范 | 输入输出对 |
| **输出格式（Format）** | 规定结构 | JSON、Markdown、表格、字数 |
| **约束（Constraints）** | 边界与限制 | "不要超过 200 字""只基于给定资料回答" |
| **意图/目标（Goal）** | 明确期望结果 | "给出可执行的 3 条建议" |

### 设计原则
- **明确具体**：模糊指令得到模糊结果；明确任务、对象、边界。
- **提供上下文**：信息越充分，输出越准确。
- **拆解复杂任务**：将大任务拆成小步骤逐步引导。
- **指定输出格式**：便于解析与后续处理。
- **使用正负向约束**：既说"要做什么"也说"不要做什么"。
- **迭代优化**：根据输出反馈不断调整。

## 3.5 提示词框架（高频考点）

常用提示词框架（只需记忆名称+核心思想，可能出单选题/判断题）：

1. **CRISPE**
   - C=Capacity and Role（能力与角色）
   - R=Insight（背景/洞察）
   - S=Statement（任务陈述）
   - P=Personality（风格）
   - E=Experiment（实验/迭代）
   - 用途：快速生成高质量提示词。

2. **CO-STAR**
   - C=Context（上下文）
   - O=Objective（目标）
   - S=Style（风格）
   - T=Tone（语调）
   - A=Audience（受众）
   - R=Response（响应格式）
   - 源自新加坡政府技术部门，结构化的任务提示模板。

3. **BROKE**（国内常见）
   - B=Background（背景）
   - R=Role（角色）
   - O=Objectives（目标）
   - K=Key Results（关键结果）
   - E=Evolve（进化/迭代）

4. **RTF（Role-Task-Format）**：角色 + 任务 + 格式，简洁三段式。

5. **ICIO**：Instruction（指令）+ Context（上下文）+ Input Data（输入数据）+ Output Indicator（输出指示）。

6. **思维链相关框架**：CoT、ToT、Self-Consistency（见 3.3）。

### 易错判断
- `提示词框架（如 CO-STAR）是固定语法，必须严格套用`（**错**——是参考模板，按需使用）。
- `CRISPE 框架中的 R 代表 Role 角色`（**错**——CRISPE 的 R 是 Insight 背景洞察，注意与 RTF 区分）。

## 3.6 负向约束与安全性

### 负向约束（Negative Constraints）
- **概念**：明确告诉模型"**不要**做什么"，用于划定行为边界，防止产生有害、错误、越界内容。
- 示例："不要编造数据""不要提及内部政策""不要输出营销话术""不要重复用户输入中的不当内容"。
- 注意：负向约束需要配合正向指令使用，才能引导出期望行为（仅说"不要"往往不够）。

### 安全风险（重点）
1. **提示注入（Prompt Injection）**：恶意用户输入试图劫持/覆盖系统提示词或改变模型行为（"忽略之前的指令……"）。
2. **越狱（Jailbreak）**：通过精心构造的提示绕过模型安全对齐，诱导有害输出。
3. **间接提示注入（Indirect Prompt Injection）**：恶意内容藏在网页、文档、外部数据中，通过 RAG 或工具调用进入上下文。
4. **数据泄露**：模型可能输出训练数据或上下文中敏感信息。
5. **幻觉（Hallucination）**：生成看似合理但错误的内容。

### 防御措施
- 输入侧：过滤恶意输入、检测注入模式。
- 输出侧：内容审核、敏感词过滤、格式校验。
- 系统侧：**权限隔离**（工具只授予最小权限）、沙箱执行、限制工具调用范围、对 Agent 的外部内容（RAG/网页）与系统指令做**角色区分**（content 与 instruction 分离）。
- 使用系统提示词声明"忽略不可信输入中的指令"。
- 人工审核（Human-in-the-loop）与审计日志。

---

# 第四部分 功能模块开发与应用（约 15%）

## 4.1 开发平台

### 低代码/可视化 AI Agent 平台
- **Dify**：开源 LLM 应用开发平台，支持工作流编排、RAG、Agent、可视化调试、插件与 API 发布。
- **Coze（扣子）**：字节跳动推出的 Agent 搭建平台，提供 Bot 商店、插件、知识库、工作流、多平台发布。
- **FastGPT**：开源知识库问答 + 工作流平台。
- **百度千帆 / 阿里百炼 / 腾讯元器**：国内云厂商大模型应用平台。
- **LangGraph Platform / LangSmith**：LangChain 系的可视化编排与可观测平台。
- 平台能力对比关注点：工作流编排、知识库/RAG、工具插件、发布渠道（Web/公众号/API）、成本与部署方式。

## 4.2 开发框架（重点）

### LangChain（生态最丰富）
- 概念：构建 LLM 应用的**框架**，核心抽象：
  - **Model**：LLM / ChatModel / Embedding。
  - **Prompt**：模板管理、示例选择器。
  - **Memory**：对话记忆（会话缓冲、摘要、向量存储记忆）。
  - **Retriever**：检索器（向量库、搜索引擎）。
  - **Tool / Tool Calling**：工具注册与调用。
  - **Chain**：串联多个环节（Prompt → LLM → 解析）。
  - **Agent / AgentExecutor**：推理 + 工具选择的执行器。

### LangGraph
- 将 Agent 流程建模为**图（节点 + 边 + 状态）**，支持循环、条件分支、人工中断（interrupt）、多 Agent 协作，适合生产级复杂编排。LangChain 生态中**新一代** Agent 编排方案。

### LlamaIndex（现在叫 LlamaCloud 的框架部分）
- 专注**数据索引与 RAG**：文档加载（Loaders）→ 切分（Splitters）→ 嵌入 → 索引（Index）→ 查询引擎（Query Engine）。
- 强调数据接入与知识库检索。

### 其他框架
- **AutoGen（微软）**：多 Agent 对话框架，GroupChat、CodeExecutor、Human 代理。
- **CrewAI**：角色化团队协作（Agent + Task + Crew + Tools）。
- **MetaGPT**：软件工程角色多 Agent 协作（标准化操作流程 SOP）。
- **Semantic Kernel（微软）**：支持多语言（.NET/Python/Java）的 AI 编排框架，Plan + Function。
- **Haystack（deepset）**：NLP 管线框架，RAG 支持良好。
- **Transformers（HuggingFace）**：模型加载与推理的底层库。

### 框架选型判断
- 需要复杂控制流/循环/人工中断 → 选 **LangGraph**。
- 快速搭建知识库问答（RAG）→ 选 **LlamaIndex** 或 Dify。
- 多 Agent 群聊/辩论 → 选 **AutoGen**。
- 角色分工团队协作 → 选 **CrewAI** / MetaGPT。

## 4.3 向量数据库（重点）

### 基本概念
- **向量（Vector）/ Embedding（嵌入）**：将文本、图像等数据转换为稠密数值向量，语义相近的内容在向量空间中距离相近。
- **向量数据库**：专门用于**存储与检索高维向量**的数据库，支持**相似度搜索（Similarity Search / ANN 近似最近邻）**，是 RAG 的核心组件。

### 相似度度量（高频考点）
- **余弦相似度（Cosine Similarity）**：向量夹角余弦，值越大越相似（-1~1），**不关心长度**，常用于文本。
- **欧氏距离（Euclidean Distance）**：值越小越相似。
- **点积（Dot Product）**：值越大越相似（内积相似度）。
- 注意：余弦相似度与欧氏距离的"大小对应关系"是**相反**的——余弦越接近 1 越相似，欧氏距离越接近 0 越相似。

### 索引算法（了解即可）
- **暴力搜索（Brute-force）**：精确但慢。
- **HNSW（分层可导航小世界图）**：基于图，速度快，召回高，最常用。
- **IVF（倒排文件）**：聚类分桶 + 桶内搜索。
- **PQ（乘积量化）**：压缩向量、减少内存。
- 常见开源/商业向量库：**FAISS**（Meta，最流行）、**Milvus**、**Chroma**、**Qdrant**、**Weaviate**、**Pinecone**（云服务）、**pgvector**（PostgreSQL 扩展）、**Elasticsearch**（也支持向量）。

### 在 Agent/RAG 中的角色
- 知识库文档 → 切分 → Embedding → 存入向量库 → 查询时把问题转为向量 → 向量库检索 Top-K 相关片段 → 作为上下文交给 LLM 生成。

## 4.4 工具调用原理（Tool Use / Function Calling，重点）

### 概念
- **工具调用（Tool Calling / Function Calling）**：让 LLM 识别需要外部工具/函数并**输出结构化的调用请求（函数名 + 参数）**，由程序执行后把结果返回给模型继续推理。
- 典型工具：搜索引擎、计算器、代码解释器、数据库查询、天气查询、HTTP API、内部系统。

### 工作流程（高频考点，按顺序记忆）
```
1. 定义工具（function schema：名称、描述、参数）
2. 将工具描述注入提示词/API
3. 用户提问
4. LLM 决定是否调用工具，输出结构化的调用请求（如 tool_calls）
5. 程序校验并执行该工具
6. 将工具执行结果返回给 LLM（作为 tool 消息）
7. LLM 综合工具结果生成最终回答（可能需要多轮工具调用）
```

### 关键概念
- **Function Schema**：OpenAI 格式的工具定义，含 `name`、`description`、`parameters`（JSON Schema）。
- **结构化输出（Structured Output）**：LLM 按 JSON Schema 输出，便于程序解析。
- **Agentic Tool Loop**：Agent 反复执行"生成工具调用 → 执行 → 观察结果 → 再生成"直到任务完成。
- 安全：**工具权限最小化**、参数校验、对工具返回内容防注入（工具输出可能携带恶意指令）。
- MCP（Model Context Protocol）：统一 Agent 与工具/数据源连接的开放协议，一个 MCP Server 暴露工具与资源，Agent 通过标准接口调用，实现"工具即插即用"。

### 易错判断
- `工具调用是指 LLM 直接执行代码操作外部系统`（**错**——LLM 只生成调用请求，由外部程序执行）。
- `LLM 总是需要调用工具来回答任何问题`（**错**——简单问题可直接回答）。

## 4.5 检索增强生成（RAG，重点）

### 概念
- **RAG（Retrieval-Augmented Generation）**：**检索 + 生成**，在让 LLM 回答前，先从**外部知识库**检索相关文档片段，拼进提示词，再让模型基于检索内容生成答案。
- 目的：**解决知识时效性、领域知识不足与幻觉问题**，让模型"开卷考试"。

### 标准流程（高频考点）
```
文档加载 → 文本切分(Chunking) → 向量化(Embedding) → 存入向量库
    → 查询时：问题向量化 → 检索 Top-K 相关片段
    → 将片段+问题组成提示词 → LLM 生成答案（可带引用来源）
```

### 关键技术点
- **切分（Chunking）**：按段落/语义/固定长度切分，块大小影响检索质量（过小信息不全，过大噪声多）。
- **Embedding 模型**：将文本转向量。
- **检索策略**：向量相似度检索、**混合检索（Hybrid Search = BM25 关键词 + 向量）**、重排序（Reranker，如 Cross-Encoder）。
- **查询改写**：把用户问题改写成更适合检索的形式（如拆分为多路查询、HyDE 假想文档）。
- **重排（Rerank）**：粗排后对 Top-K 精排，提升相关性。

### RAG 的挑战与优化
- 检索质量差 → 优化切分、混合检索、重排、多路召回。
- 幻觉 → 让模型只基于检索内容回答、要求引用来源、降低温度。
- 知识更新 → 增量更新向量库。
- 延迟 → 缓存、优化检索。
- 评估指标：**检索召回率、命中率、回答忠实度（Faithfulness）、答案相关性与准确性**。

### RAG vs 微调（Fine-tuning）
| 维度 | RAG | 微调 |
|------|-----|------|
| 适用场景 | 知识密集型、需实时更新的知识 | 改变模型能力/风格/格式 |
| 更新成本 | 低（更新知识库即可） | 高（重新训练） |
| 幻觉控制 | 较好（基于检索） | 一般 |
| 成本 | 每次检索+生成 | 一次性训练成本 |
- 判断题：`RAG 与微调互斥，只能二选一`（**错**——可结合使用）。

## 4.6 API 与数据集成知识

### API 基础
- **API（应用程序接口）**：软件之间交互的接口。
- **RESTful API**：基于 HTTP，资源用 URL 标识，方法：**GET（查）、POST（增）、PUT（改）、DELETE（删）、PATCH（部分修改）**。
- **HTTP 状态码**：2xx 成功（200 OK）、3xx 重定向、4xx 客户端错误（**404 未找到、401 未认证、403 无权限、400 参数错误**）、5xx 服务端错误（500、502 网关错误、503 服务不可用）。
- **请求头**：`Authorization`、`Content-Type`（application/json 等）。
- 数据格式：**JSON**（键值对，最常用）、XML、表单。

### 认证与授权（高频考点）
- **API Key**：简单密钥，随请求头传递。
- **OAuth 2.0**：授权框架，常见流程授权码模式，用于第三方应用访问用户资源（"授权登录"）。
- **JWT（JSON Web Token）**：无状态令牌，含签名，用于身份认证与信息交换，无需服务端存储会话。
- 基本认证（Basic Auth）：用户名:密码 Base64。
- 三者对比：API Key 最简单；OAuth 用于授权第三方；JWT 适合分布式无状态认证。

### 数据集成
- Webhook：服务端主动回调（事件通知），与轮询（Polling）相对。
- 数据管道 ETL：抽取（Extract）、转换（Transform）、加载（Load）。
- 数据库集成：通过 SQL/连接器读写数据库；向量库用于非结构化语义检索。
- 企业集成常用协议/中间件：消息队列（Kafka、RabbitMQ）、REST、gRPC。

## 4.7 长短期记忆机制（Memory，重点）

### 记忆的分类（核心考点）
| 类型 | 说明 | 实现方式 | 特点 |
|------|------|----------|------|
| **短期记忆（Short-term / Working Memory）** | 当前对话上下文与任务状态 | 上下文窗口、对话历史列表、工作内存状态 | 容量有限、随对话滑动/截断 |
| **长期记忆（Long-term Memory）** | 跨会话持久化的知识与偏好 | 向量数据库、关系数据库、KV 存储、文件 | 可长期保存、按需检索 |
| **情景记忆（Episodic）** | 过去经历的事件 | 历史对话/操作记录 | 记录发生了什么 |
| **语义记忆（Semantic）** | 事实与知识 | 知识库、向量库 | 是什么/怎么做 |
| **程序性记忆（Procedural）** | 技能/流程 | 脚本、规则、技能库 | 怎么做 |

### 记忆管理技术
- **上下文压缩/截断**：超出上下文窗口时丢弃早期内容。
- **记忆摘要（Summarization）**：将历史对话压缩为摘要存储，重要信息不丢。
- **向量检索记忆（Vector Memory）**：历史内容向量化，按相关性检索回放。
- **记忆写入与遗忘**：判定哪些信息值得长期保存、何时过期清理。
- **会话缓冲（Conversation Buffer / Buffer Window）**：保存最近 N 轮对话。
- **工作记忆（Working Memory / State）**：Agent 当前任务执行的状态、中间变量、计划进度。

### 记忆与上下文的关系
- 上下文窗口提供**短期**工作空间；长期记忆需要**检索**才能进入上下文。
- 判断题：`Agent 的记忆容量就等于 LLM 的上下文窗口`（**错**——上下文窗口只是短期记忆，长期记忆通过外部存储+检索实现）。

### 记忆在 Agent 循环中的作用
```
感知/输入 → 读取相关记忆（长期检索）→ 结合当前上下文推理 → 行动
     → 更新记忆（写入新知识/更新状态）
```

---

# 附录 A：高频易错判断题汇总（速记）

1. 操作系统属于系统软件。（√）
2. 进程是 CPU 调度的基本单位。（×，线程才是）
3. 内存断电后数据丢失，外存断电后数据保留。（√）
4. TCP 是面向连接的可靠传输协议。（√）
5. UDP 提供可靠的端到端传输。（×）
6. HTTPS 在 HTTP 基础上增加了 SSL/TLS 加密。（√）
7. IaaS 只提供基础设施，用户自己管理操作系统以上部分。（√）
8. SaaS 用户需要自己安装部署软件。（×，直接使用）
9. 大数据的"4V"指 Volume、Velocity、Variety、Value。（√）
10. 深度学习是人工智能的一个子集。（√）
11. Python 是编译型语言。（×，解释型）
12. Python 元组与列表一样可变。（×，元组不可变）
13. 《个人信息保护法》确立了"告知-同意"原则。（√）
14. GPL 许可证要求衍生作品必须开源。（√）
15. LLM 本身就具备自主行动能力，可以直接称为 Agent。（×）
16. Agent 的核心能力包括感知、规划、记忆、行动、工具使用。（√）
17. ReAct 模式让模型交替进行推理与行动。（√）
18. 多 Agent 系统一定能比单 Agent 效果更好。（×，取决于任务与协调成本）
19. 系统提示词的优先级高于用户提示词。（√）
20. 添加"让我们一步步思考"即可激发思维链。（√，Zero-shot CoT）
21. 思维链只适用于数学题。（×）
22. Few-shot 学习需要更新模型参数。（×，属于上下文学习）
23. 负向约束（"不要做什么"）可以单独使用且效果最佳。（×，应与正向指令结合）
24. 提示注入是用户在提示词中加入恶意指令以劫持模型行为。（√）
25. 余弦相似度越接近 1，文本越相似。（√）
26. 欧氏距离越小，向量越相似。（√）
27. HNSW 是一种暴力检索算法。（×，是基于图的近似最近邻算法）
28. 工具调用时，LLM 直接执行外部函数。（×，由外部程序执行）
29. RAG 可以降低模型幻觉并引入外部知识。（√）
30. RAG 与模型微调不能同时使用。（×）
31. RAG 中检索到的内容应原样拼接，无需重排优化。（×）
32. JWT 是无状态令牌，服务端无需存储会话。（√）
33. OAuth 2.0 是一种身份认证协议而不是授权框架。（×，它是授权框架）
34. Agent 的长期记忆必须通过外部存储与检索实现，不等于上下文窗口。（√）
35. 记忆摘要是将历史对话压缩保存，用于节省上下文空间。（√）

---

# 附录 B：11 天备考计划（8月10日—8月20日）

| 日期 | 内容 | 建议 |
|------|------|------|
| 8/10 | 通读手册第一部分（基础知识） | 重点：OS、网络协议、云计算服务模式、Python 数据结构 |
| 8/11 | 精读第二部分 2.1-2.3（Agent 概念/架构/多 Agent） | 画"Agent 循环"与多 Agent 协作图 |
| 8/12 | 精读第二部分 2.4-2.7（组件/工作流/评估/治理） | 记忆评估维度表、编排 vs 协作 |
| 8/13 | 精读第三部分（提示词工程） | 区分 System/User、CoT 变体、三个框架名对应 |
| 8/14 | 精读第四部分（向量库/工具调用/RAG/记忆） | 手写 RAG 流程与工具调用流程各一遍 |
| 8/15 | 第一轮综合复习 + 附录 A 判断题自测 | 错题记录到笔记本 |
| 8/16 | 自拟模拟卷：单选 30 题 + 多选 5 题 + 判断 10 题 | 限时 45 分钟完成并订正 |
| 8/17 | 第二遍扫读手册，重点看标记与易错点 | 查漏补缺 |
| 8/18 | 自拟模拟卷：单选 70 题限时 40 分钟 | 训练速度 |
| 8/19 | 薄弱板块专项突破（多选训练 + 记忆表格） | 多选最易失分，专项练 |
| 8/20 | 轻量回顾：附录 A + 高频考点清单 + 休息 | 早睡，调整状态 |
| 8/21 | 考试 | 按时间分配答题，先易后难 |

## 考前最后一页速记卡

- Agent = LLM + 规划 + 记忆 + 工具 + 执行（感知-规划-行动循环）。
- ReAct = Thought → Action → Observation 循环。
- 多 Agent 模式：流水线 / 分层 / 群聊 / 辩论 / 委派。
- 评估维度：准确、效率、成本、鲁棒、安全、可解释、可控。
- CoT = 让模型分步推理；Zero-shot CoT = "让我们一步步思考"；Self-Consistency = 多次采样投票。
- 提示词要素：角色、任务、上下文、示例、输出格式、约束。
- 框架：CRISPE、CO-STAR、BROKE、RTF、ICIO。
- 向量库：HNSW/IVF 索引，余弦相似度（越大越相似）、欧氏距离（越小越相似）。
- RAG 流程：加载→切分→嵌入→存储→检索→重排→生成。
- 工具调用：LLM 生成结构化请求 → 程序执行 → 结果返回模型。
- 记忆：短期=上下文窗口，长期=向量库/数据库检索；摘要压缩旧对话。
- 云服务：IaaS/PaaS/SaaS；大数据 4V。
- 端口：HTTP 80、HTTPS 443、SSH 22、DNS 53、FTP 21。
